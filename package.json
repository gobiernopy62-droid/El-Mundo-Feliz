{
  "name": "el-mundo-feliz",
  "version": "1.0.0",
  "scripts": {
    "build": "echo 'No se necesita build tradicional porque es HTML puro'",
    "sync": "npx cap sync"
  },
  "devDependencies": {
    "@capacitor/cli": "^latest",
    "@capacitor/core": "^latest",
    "@capacitor/android": "^latest",
    "@capacitor/assets": "^latest"
  }
}